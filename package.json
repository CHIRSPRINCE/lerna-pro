{
  "name": "study",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "i": "yarn install && lerna bootstrap",
    "pub": "lerna publish",
    "palp": "lerna publish --pre-dist-tag alpha --preid alpha",
    "pbeta": "lerna publish --pre-dist-tag beta --preid beta"
  }
}
